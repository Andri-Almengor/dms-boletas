import { readFileSync, writeFileSync } from 'node:fs';

function replaceOnce(source, before, after, label) {
  if (!source.includes(before)) throw new Error(`No se encontró bloque para ${label}`);
  return source.replace(before, after);
}

const cameraPath = 'gateway-agent/src/camera/onvif-camera-control.js';
let camera = readFileSync(cameraPath, 'utf8');

camera = replaceOnce(
  camera,
  `function soapEnvelope(body, auth) {\n  return \`<?xml version="1.0" encoding="UTF-8"?><s:Envelope xmlns:s="http://www.w3.org/2003/05/soap-envelope" xmlns:tds="http://www.onvif.org/ver10/device/wsdl" xmlns:trt="http://www.onvif.org/ver10/media/wsdl" xmlns:tptz="http://www.onvif.org/ver20/ptz/wsdl" xmlns:tt="http://www.onvif.org/ver10/schema">\${wsSecurityHeader(auth)}<s:Body>\${body}</s:Body></s:Envelope>\`;\n}\n`,
  `function soapEnvelope(body, auth) {\n  return \`<?xml version="1.0" encoding="UTF-8"?><s:Envelope xmlns:s="http://www.w3.org/2003/05/soap-envelope" xmlns:tds="http://www.onvif.org/ver10/device/wsdl" xmlns:trt="http://www.onvif.org/ver10/media/wsdl" xmlns:tr2="http://www.onvif.org/ver20/media/wsdl" xmlns:tptz="http://www.onvif.org/ver20/ptz/wsdl" xmlns:tt="http://www.onvif.org/ver10/schema">\${wsSecurityHeader(auth)}<s:Body>\${body}</s:Body></s:Envelope>\`;\n}\n`,
  'namespace Media2',
);

const oldServices = `function serviceUrlFromServices(xml, namespacePattern, expectedIp) {\n  for (const service of sections(xml, 'Service')) {\n    const namespace = localTag(service, 'Namespace');\n    if (!namespacePattern.test(namespace)) continue;\n    const url = sameCameraUrl(localTag(service, 'XAddr'), expectedIp);\n    if (url) return url;\n  }\n  return null;\n}\n\nasync function serviceEndpoints(device, auth, timeoutMs) {\n  const ip = clean(device.ipAddress, 100);\n  const deviceUrl = resolveDeviceEndpoint(device);\n\n  try {\n    // Omitir Category es equivalente a solicitar todas las capacidades según ONVIF\n    // y evita firmwares que rechazan explícitamente Category=All con InvalidOperation.\n    const xml = await soapCall(\n      deviceUrl,\n      'http://www.onvif.org/ver10/device/wsdl/GetCapabilities',\n      '<tds:GetCapabilities/>',\n      auth,\n      timeoutMs,\n    );\n    const mediaUrl = sameCameraUrl(localTag(section(xml, 'Media'), 'XAddr'), ip);\n    const ptzUrl = sameCameraUrl(localTag(section(xml, 'PTZ'), 'XAddr'), ip);\n    return { deviceUrl, mediaUrl, ptzUrl, discoveryMethod: 'GET_CAPABILITIES' };\n  } catch (error) {\n    if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    if (!isOptionalOnvifActionUnsupported(error)) throw error;\n  }\n\n  try {\n    // GetCapabilities está deprecado en ONVIF moderno. Algunos firmwares/OEM,\n    // incluyendo variantes compatibles de TruVision, solo exponen correctamente\n    // la enumeración genérica GetServices.\n    const xml = await soapCall(\n      deviceUrl,\n      'http://www.onvif.org/ver10/device/wsdl/GetServices',\n      '<tds:GetServices><tds:IncludeCapability>false</tds:IncludeCapability></tds:GetServices>',\n      auth,\n      timeoutMs,\n    );\n    const mediaUrl = serviceUrlFromServices(xml, /\\/ver(?:10|20)\\/media\\/wsdl\\/?$/i, ip);\n    const ptzUrl = serviceUrlFromServices(xml, /\\/ver(?:10|20)\\/ptz\\/wsdl\\/?$/i, ip);\n    return { deviceUrl, mediaUrl, ptzUrl, discoveryMethod: 'GET_SERVICES' };\n  } catch (error) {\n    if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    if (!isOptionalOnvifActionUnsupported(error)) throw error;\n    // La autenticación ya fue aceptada; no se considera una contraseña errónea ni\n    // se activa una búsqueda de credenciales. La cámara simplemente no implementa\n    // los métodos opcionales de descubrimiento que necesitamos.\n    return { deviceUrl, mediaUrl: null, ptzUrl: null, discoveryMethod: 'DEVICE_ONLY' };\n  }\n}\n`;

const newServices = `function serviceDescriptorFromServices(xml, namespacePattern, expectedIp) {\n  for (const service of sections(xml, 'Service')) {\n    const namespace = localTag(service, 'Namespace');\n    if (!namespacePattern.test(namespace)) continue;\n    const url = sameCameraUrl(localTag(service, 'XAddr'), expectedIp);\n    if (url) return { url, namespace };\n  }\n  return null;\n}\n\nasync function getServicesEndpoints(deviceUrl, ip, auth, timeoutMs) {\n  const xml = await soapCall(\n    deviceUrl,\n    'http://www.onvif.org/ver10/device/wsdl/GetServices',\n    '<tds:GetServices><tds:IncludeCapability>false</tds:IncludeCapability></tds:GetServices>',\n    auth,\n    timeoutMs,\n  );\n  const media = serviceDescriptorFromServices(xml, /\\/ver(?:10|20)\\/media\\/wsdl\\/?$/i, ip);\n  const ptz = serviceDescriptorFromServices(xml, /\\/ver(?:10|20)\\/ptz\\/wsdl\\/?$/i, ip);\n  return {\n    mediaUrl: media?.url || null,\n    mediaVersion: /\\/ver20\\/media\\/wsdl\\/?$/i.test(media?.namespace || '') ? 'MEDIA2' : 'MEDIA1',\n    ptzUrl: ptz?.url || null,\n  };\n}\n\nasync function serviceEndpoints(device, auth, timeoutMs) {\n  const ip = clean(device.ipAddress, 100);\n  const deviceUrl = resolveDeviceEndpoint(device);\n  let mediaUrl = null;\n  let mediaVersion = 'MEDIA1';\n  let ptzUrl = null;\n  let capabilitiesAccepted = false;\n\n  try {\n    const xml = await soapCall(\n      deviceUrl,\n      'http://www.onvif.org/ver10/device/wsdl/GetCapabilities',\n      '<tds:GetCapabilities/>',\n      auth,\n      timeoutMs,\n    );\n    capabilitiesAccepted = true;\n    mediaUrl = sameCameraUrl(localTag(section(xml, 'Media'), 'XAddr'), ip);\n    ptzUrl = sameCameraUrl(localTag(section(xml, 'PTZ'), 'XAddr'), ip);\n  } catch (error) {\n    if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    if (!isOptionalOnvifActionUnsupported(error)) throw error;\n  }\n\n  // Incluso cuando GetCapabilities responde 200, algunos firmwares devuelven una\n  // lista incompleta. GetServices complementa Media/Media2/PTZ sin considerar eso\n  // un fallo de autenticación.\n  if (!mediaUrl || !ptzUrl) {\n    try {\n      const services = await getServicesEndpoints(deviceUrl, ip, auth, timeoutMs);\n      mediaUrl ||= services.mediaUrl;\n      if (services.mediaUrl && !capabilitiesAccepted) mediaVersion = services.mediaVersion;\n      if (!mediaUrl || mediaUrl === services.mediaUrl) mediaVersion = services.mediaVersion;\n      ptzUrl ||= services.ptzUrl;\n      return {\n        deviceUrl,\n        mediaUrl,\n        mediaVersion,\n        ptzUrl,\n        discoveryMethod: capabilitiesAccepted ? 'GET_CAPABILITIES+GET_SERVICES' : 'GET_SERVICES',\n      };\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n      if (!capabilitiesAccepted && !isOptionalOnvifActionUnsupported(error)) throw error;\n    }\n  }\n\n  return {\n    deviceUrl,\n    mediaUrl,\n    mediaVersion,\n    ptzUrl,\n    discoveryMethod: capabilitiesAccepted ? 'GET_CAPABILITIES' : 'DEVICE_ONLY',\n  };\n}\n`;
camera = replaceOnce(camera, oldServices, newServices, 'descubrimiento de servicios');

const oldMedia = `async function mediaProfile(mediaUrl, auth, timeoutMs) {\n  if (!mediaUrl) return null;\n  const profilesXml = await soapCall(\n    mediaUrl,\n    'http://www.onvif.org/ver10/media/wsdl/GetProfiles',\n    '<trt:GetProfiles/>',\n    auth,\n    timeoutMs,\n  );\n  const profileToken = firstToken(profilesXml, 'Profiles');\n  if (!profileToken) return null;\n  const profileSection = section(profilesXml, 'Profiles');\n  return {\n    profileToken,\n    ptzConfigurationToken: firstToken(profileSection, 'PTZConfiguration'),\n  };\n}\n\nasync function snapshotUri(mediaUrl, profileToken, deviceIp, auth, timeoutMs) {\n  if (!mediaUrl || !profileToken) return null;\n  const xml = await soapCall(\n    mediaUrl,\n    'http://www.onvif.org/ver10/media/wsdl/GetSnapshotUri',\n    \`<trt:GetSnapshotUri><trt:ProfileToken>\${escapeXml(profileToken)}</trt:ProfileToken></trt:GetSnapshotUri>\`,\n    auth,\n    timeoutMs,\n  );\n  return sameCameraUrl(localTag(xml, 'Uri'), deviceIp);\n}\n`;

const newMedia = `function mediaDialect(mediaVersion = 'MEDIA1') {\n  if (mediaVersion === 'MEDIA2') {\n    return {\n      getProfilesAction: 'http://www.onvif.org/ver20/media/wsdl/GetProfiles',\n      getProfilesBody: '<tr2:GetProfiles><tr2:Type>All</tr2:Type></tr2:GetProfiles>',\n      snapshotAction: 'http://www.onvif.org/ver20/media/wsdl/GetSnapshotUri',\n      snapshotBody: (token) => \`<tr2:GetSnapshotUri><tr2:ProfileToken>\${escapeXml(token)}</tr2:ProfileToken></tr2:GetSnapshotUri>\`,\n    };\n  }\n  return {\n    getProfilesAction: 'http://www.onvif.org/ver10/media/wsdl/GetProfiles',\n    getProfilesBody: '<trt:GetProfiles/>',\n    snapshotAction: 'http://www.onvif.org/ver10/media/wsdl/GetSnapshotUri',\n    snapshotBody: (token) => \`<trt:GetSnapshotUri><trt:ProfileToken>\${escapeXml(token)}</trt:ProfileToken></trt:GetSnapshotUri>\`,\n  };\n}\n\nasync function mediaProfile(mediaUrl, auth, timeoutMs, mediaVersion = 'MEDIA1') {\n  if (!mediaUrl) return null;\n  const dialect = mediaDialect(mediaVersion);\n  const profilesXml = await soapCall(\n    mediaUrl,\n    dialect.getProfilesAction,\n    dialect.getProfilesBody,\n    auth,\n    timeoutMs,\n  );\n  const profileToken = firstToken(profilesXml, 'Profiles');\n  if (!profileToken) return null;\n  const profileSection = section(profilesXml, 'Profiles');\n  return {\n    profileToken,\n    ptzConfigurationToken: firstToken(profileSection, 'PTZConfiguration'),\n  };\n}\n\nasync function snapshotUri(mediaUrl, profileToken, deviceIp, auth, timeoutMs, mediaVersion = 'MEDIA1') {\n  if (!mediaUrl || !profileToken) return null;\n  const dialect = mediaDialect(mediaVersion);\n  const xml = await soapCall(\n    mediaUrl,\n    dialect.snapshotAction,\n    dialect.snapshotBody(profileToken),\n    auth,\n    timeoutMs,\n  );\n  return sameCameraUrl(localTag(xml, 'Uri'), deviceIp);\n}\n\nfunction isHanwhaDevice(device = {}) {\n  const signature = \`${String(device.manufacturer || '')} \${String(device.model || '')}\`.toUpperCase();\n  return /HANWHA|WISENET|SAMSUNG TECHWIN|\\bPND-|\\bPNV-|\\bXND-|\\bXNV-|\\bQND-|\\bQNV-/.test(signature);\n}\n\nfunction cameraHttpBaseUrl(device = {}) {\n  const ip = clean(device.ipAddress, 100);\n  const ports = new Set((device.openPorts || []).map(Number));\n  const protocol = ports.has(80) ? 'http:' : ports.has(443) ? 'https:' : resolveDeviceEndpoint(device).protocol;\n  return new URL(\`\${protocol}//\${ip}/\`);\n}\n\nfunction imageResponseToSnapshot(response, transport) {\n  const contentType = clean(String(response.headers['content-type'] || '').split(';')[0], 80).toLowerCase();\n  const jpegMagic = response.buffer?.length >= 2 && response.buffer[0] === 0xff && response.buffer[1] === 0xd8;\n  const pngMagic = response.buffer?.length >= 8 && response.buffer.subarray(1, 4).toString('ascii') === 'PNG';\n  if (!jpegMagic && !pngMagic && !contentType.startsWith('image/')) {\n    throw new CameraControlError('CAMERA_SNAPSHOT_INVALID', 'La cámara respondió correctamente, pero el contenido no era una imagen.');\n  }\n  const mimeType = contentType === 'image/png' || pngMagic ? 'image/png' : 'image/jpeg';\n  return {\n    mimeType,\n    bytes: response.buffer.length,\n    dataBase64: response.buffer.toString('base64'),\n    capturedAt: new Date().toISOString(),\n    transport,\n  };\n}\n\nasync function hanwhaSunapiSnapshot(device, auth, timeoutMs) {\n  const url = new URL('/stw-cgi/video.cgi?msubmenu=snapshot&action=view', cameraHttpBaseUrl(device));\n  const response = await requestWithHttpAuthentication(url, {\n    method: 'GET',\n    timeoutMs,\n    maxBytes: 3 * 1024 * 1024,\n    headers: { Accept: 'image/jpeg,*/*;q=0.1' },\n  }, auth);\n  if (isAuthRejected(response)) {\n    throw new CameraControlError('CAMERA_AUTH_REJECTED', 'La cámara Hanwha rechazó la credencial al solicitar la captura SUNAPI.', response.statusCode);\n  }\n  if (response.statusCode < 200 || response.statusCode >= 300) {\n    throw new CameraControlError('CAMERA_VENDOR_SNAPSHOT_FAILED', \`SUNAPI respondió \${response.statusCode} al solicitar la captura.\`, response.statusCode);\n  }\n  return imageResponseToSnapshot(response, 'HANWHA_SUNAPI');\n}\n`;
camera = replaceOnce(camera, oldMedia, newMedia, 'Media1/Media2 y SUNAPI');

const oldCapabilities = `async function getCapabilities(device, auth, timeoutMs) {\n  const endpoints = await serviceEndpoints(device, auth, timeoutMs);\n  let profile = null;\n  if (endpoints.mediaUrl) {\n    try {\n      profile = await mediaProfile(endpoints.mediaUrl, auth, timeoutMs);\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n      // Un servicio Media parcialmente implementado no debe tumbar toda la\n      // consulta de capacidades. Se reporta la función concreta como no disponible.\n    }\n  }\n  let snapshotSupported = false;\n  if (profile?.profileToken && endpoints.mediaUrl) {\n    try {\n      snapshotSupported = Boolean(await snapshotUri(endpoints.mediaUrl, profile.profileToken, device.ipAddress, auth, timeoutMs));\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    }\n  }\n  const ptz = await ptzCapabilities(endpoints.ptzUrl, auth, timeoutMs);\n  return {\n    endpoints,\n    profile,\n    capabilities: {\n      authentication: true,\n      snapshot: snapshotSupported,\n      ptz: ptz.ptz,\n      continuousZoom: ptz.continuousZoom,\n      homePosition: ptz.homePosition,\n      reboot: true,\n    },\n  };\n}\n`;

const newCapabilities = `async function getCapabilities(device, auth, timeoutMs) {\n  const endpoints = await serviceEndpoints(device, auth, timeoutMs);\n  let profile = null;\n  if (endpoints.mediaUrl) {\n    try {\n      profile = await mediaProfile(endpoints.mediaUrl, auth, timeoutMs, endpoints.mediaVersion);\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    }\n  }\n\n  let snapshotSupported = false;\n  let snapshotTransport = '';\n  if (profile?.profileToken && endpoints.mediaUrl) {\n    try {\n      snapshotSupported = Boolean(await snapshotUri(\n        endpoints.mediaUrl,\n        profile.profileToken,\n        device.ipAddress,\n        auth,\n        timeoutMs,\n        endpoints.mediaVersion,\n      ));\n      if (snapshotSupported) snapshotTransport = endpoints.mediaVersion === 'MEDIA2' ? 'ONVIF_MEDIA2' : 'ONVIF_MEDIA1';\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    }\n  }\n\n  // Hanwha documenta una ruta SUNAPI de snapshot independiente de ONVIF.\n  // Esto evita declarar falsamente "No" cuando GetSnapshotUri no está publicado.\n  if (!snapshotSupported && isHanwhaDevice(device)) {\n    try {\n      await hanwhaSunapiSnapshot(device, auth, timeoutMs);\n      snapshotSupported = true;\n      snapshotTransport = 'HANWHA_SUNAPI';\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    }\n  }\n\n  const ptz = await ptzCapabilities(endpoints.ptzUrl, auth, timeoutMs);\n  const pndA7082rv = /^PND-A7082RV$/i.test(clean(device.model, 160));\n  return {\n    endpoints,\n    profile,\n    capabilities: {\n      authentication: true,\n      snapshot: snapshotSupported,\n      snapshotStatus: snapshotSupported ? 'AVAILABLE' : 'UNDETERMINED',\n      snapshotTransport,\n      ptz: ptz.ptz,\n      continuousZoom: ptz.continuousZoom,\n      opticalZoom: pndA7082rv,\n      opticalZoomRatio: pndA7082rv ? '2.0x' : '',\n      zoomStatus: ptz.continuousZoom ? 'AVAILABLE_ONVIF_PTZ' : pndA7082rv ? 'HARDWARE_PRESENT_CONTROL_NOT_CONFIRMED' : 'UNDETERMINED',\n      homePosition: ptz.homePosition,\n      reboot: true,\n      discoveryMethod: endpoints.discoveryMethod,\n      mediaVersion: endpoints.mediaVersion || '',\n    },\n  };\n}\n`;
camera = replaceOnce(camera, oldCapabilities, newCapabilities, 'capabilities');

const oldSnapshot = `async function getSnapshot(device, auth, timeoutMs) {\n  const resolved = await getCapabilities(device, auth, timeoutMs);\n  if (!resolved.capabilities.snapshot || !resolved.endpoints.mediaUrl || !resolved.profile?.profileToken) {\n    throw new CameraControlError('CAMERA_SNAPSHOT_UNSUPPORTED', 'La cámara no expone una captura ONVIF utilizable con este perfil.');\n  }\n  const uri = await snapshotUri(resolved.endpoints.mediaUrl, resolved.profile.profileToken, device.ipAddress, auth, timeoutMs);\n  if (!uri) throw new CameraControlError('CAMERA_SNAPSHOT_URI_REJECTED', 'La cámara devolvió una URL de captura fuera de su propia IP.');\n  const response = await requestWithHttpAuthentication(uri, {\n    method: 'GET',\n    timeoutMs,\n    maxBytes: 3 * 1024 * 1024,\n    headers: { Accept: 'image/jpeg,image/png;q=0.8,*/*;q=0.1' },\n  }, auth);\n  if (isAuthRejected(response)) throw new CameraControlError('CAMERA_AUTH_REJECTED', 'La cámara rechazó la credencial al solicitar la captura.', response.statusCode);\n  if (response.statusCode < 200 || response.statusCode >= 300) {\n    throw new CameraControlError('CAMERA_SNAPSHOT_FAILED', \`La cámara respondió \${response.statusCode} al solicitar la captura.\`, response.statusCode);\n  }\n  const contentType = clean(String(response.headers['content-type'] || '').split(';')[0], 80).toLowerCase();\n  const mimeType = contentType === 'image/png' ? 'image/png' : 'image/jpeg';\n  return {\n    mimeType,\n    bytes: response.buffer.length,\n    dataBase64: response.buffer.toString('base64'),\n    capturedAt: new Date().toISOString(),\n  };\n}\n`;

const newSnapshot = `async function getSnapshot(device, auth, timeoutMs) {\n  const resolved = await getCapabilities(device, auth, timeoutMs);\n\n  if (resolved.endpoints.mediaUrl && resolved.profile?.profileToken) {\n    try {\n      const uri = await snapshotUri(\n        resolved.endpoints.mediaUrl,\n        resolved.profile.profileToken,\n        device.ipAddress,\n        auth,\n        timeoutMs,\n        resolved.endpoints.mediaVersion,\n      );\n      if (uri) {\n        const response = await requestWithHttpAuthentication(uri, {\n          method: 'GET',\n          timeoutMs,\n          maxBytes: 3 * 1024 * 1024,\n          headers: { Accept: 'image/jpeg,image/png;q=0.8,*/*;q=0.1' },\n        }, auth);\n        if (isAuthRejected(response)) throw new CameraControlError('CAMERA_AUTH_REJECTED', 'La cámara rechazó la credencial al solicitar la captura.', response.statusCode);\n        if (response.statusCode >= 200 && response.statusCode < 300) {\n          return imageResponseToSnapshot(response, resolved.endpoints.mediaVersion === 'MEDIA2' ? 'ONVIF_MEDIA2' : 'ONVIF_MEDIA1');\n        }\n      }\n    } catch (error) {\n      if (error.code === 'CAMERA_AUTH_REJECTED') throw error;\n    }\n  }\n\n  if (isHanwhaDevice(device)) return hanwhaSunapiSnapshot(device, auth, timeoutMs);\n\n  throw new CameraControlError(\n    'CAMERA_SNAPSHOT_UNDETERMINED',\n    'La cámara no publicó una ruta de captura utilizable por ONVIF y todavía no hay un adaptador de fabricante confirmado para este modelo.',\n  );\n}\n`;
camera = replaceOnce(camera, oldSnapshot, newSnapshot, 'snapshot');
writeFileSync(cameraPath, camera);

const formatterPath = 'backend/src/services/integration-gateway-answer-format.patch.js';
let formatter = readFileSync(formatterPath, 'utf8');
formatter = replaceOnce(
  formatter,
  `function capabilityDetails(capabilities = {}) {\n  return [\n    \`• Captura de imagen: \${capabilities.snapshot ? 'Sí' : 'No'}\`,\n    \`• PTZ: \${capabilities.ptz ? 'Sí' : 'No'}\`,\n    \`• Zoom continuo: \${capabilities.continuousZoom ? 'Sí' : 'No'}\`,\n    \`• Posición Home: \${capabilities.homePosition ? 'Sí' : 'No'}\`,\n    \`• Reinicio ONVIF: \${capabilities.reboot ? 'Sí' : 'No'}\`,\n  ];\n}\n`,
  `function capabilityDetails(capabilities = {}) {\n  const snapshotTransport = capabilities.snapshotTransport ? \` (\${capabilities.snapshotTransport})\` : '';\n  const snapshot = capabilities.snapshot\n    ? \`Sí\${snapshotTransport}\`\n    : capabilities.snapshotStatus === 'UNDETERMINED' ? 'No determinado todavía' : 'No';\n  const details = [\n    \`• Captura de imagen: \${snapshot}\`,\n    \`• PTZ ONVIF: \${capabilities.ptz ? 'Sí' : 'No detectado'}\`,\n    \`• Zoom PTZ continuo: \${capabilities.continuousZoom ? 'Sí' : 'No detectado'}\`,\n  ];\n  if (capabilities.opticalZoom) {\n    details.push(\`• Zoom óptico del lente: Sí\${capabilities.opticalZoomRatio ? ` (\${capabilities.opticalZoomRatio})` : ''}; control remoto del lente aún no confirmado\`);\n  }\n  details.push(\n    \`• Posición Home: \${capabilities.homePosition ? 'Sí' : 'No detectada'}\`,\n    \`• Reinicio ONVIF: \${capabilities.reboot ? 'Sí' : 'No'}\`,\n  );\n  return details;\n}\n`,
  'formato capacidades',
);
formatter = formatter.replace(
  `'Acciones disponibles según la respuesta ONVIF de la cámara:',`,
  `'Acciones detectadas mediante ONVIF y adaptadores compatibles del fabricante:',`,
);
writeFileSync(formatterPath, formatter);

const testPath = 'tests/characterization/gateway-camera-control-assistant.test.mjs';
let tests = readFileSync(testPath, 'utf8');
tests = tests.replace(
  `  assert.match(camera, /discoveryMethod: 'GET_SERVICES'/);\n`,
  `  assert.match(camera, /GET_CAPABILITIES\\+GET_SERVICES/);\n  assert.match(camera, /MEDIA2/);\n  assert.match(camera, /HANWHA_SUNAPI/);\n  assert.match(camera, /stw-cgi\\/video\\.cgi\\?msubmenu=snapshot&action=view/);\n`,
);
tests = tests.replace(
  `  assert.match(formatter, /gatewayCameraCapabilities/);\n`,
  `  assert.match(formatter, /gatewayCameraCapabilities/);\n  assert.match(formatter, /No determinado todavía/);\n  assert.match(formatter, /Zoom óptico del lente/);\n`,
);
writeFileSync(testPath, tests);
