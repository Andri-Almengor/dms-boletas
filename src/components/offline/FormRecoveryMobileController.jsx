/* Compatibilidad con la arquitectura existente.
 * El indicador de recuperación ahora es compacto por CSS en todos los tamaños,
 * por lo que ya no necesita observar el DOM ni alternar paneles expandibles. */
export default function FormRecoveryMobileController() {
  return null;
}
