import React,{useEffect,useMemo,useState}from'react';
import{Link,useNavigate,useParams}from'react-router-dom';
import{useAuth}from'../../AuthContext';
import Icon from'../../components/common/Icon';
import{getMaintenanceCategory}from'../../config/maintenanceCategories';
import{MODULE_ROUTES,pick,requestAvailable}from'../../services/moduleApi';
function date(value){if(!value)return'Sin fecha';const parsed=new Date(value);if(Number.isNaN(parsed.getTime()))return String(value);return new Intl.DateTimeFormat('es-CR',{dateStyle:'long'}).format(parsed)}
function answerClass(value){const text=String(value||'').toLowerCase();return text.startsWith('sí')||text==='correcto'?'is-good':text?'is-bad':''}
export default function MaintenanceDetailPage(){
 const{maintenanceId}=useParams(),navigate=useNavigate(),{sessionToken,hasPermission}=useAuth();
 const isAdmin=hasPermission('USUARIOS_GESTIONAR')||hasPermission('MANTENIMIENTOS_ELIMINAR'),canEdit=hasPermission('MANTENIMIENTOS_EDITAR')||hasPermission('BOLETAS_EDITAR'),canFinalize=hasPermission('MANTENIMIENTOS_FINALIZAR')||hasPermission('BOLETAS_FINALIZAR')||canEdit;
 const[data,setData]=useState(null),[loading,setLoading]=useState(true),[working,setWorking]=useState(''),[error,setError]=useState('');
 async function load(){setLoading(true);setError('');try{setData(await requestAvailable(MODULE_ROUTES.maintenance.get,{maintenanceId},sessionToken))}catch(err){setError(err.message)}finally{setLoading(false)}}
 useEffect(()=>{load()},[maintenanceId,sessionToken]);
 const row=data?.mantenimiento||data||{},devices=data?.dispositivos||data?.devices||[],status=String(pick(row,['Estado'],'PENDIENTE')).toUpperCase();
 const grouped=useMemo(()=>devices.reduce((map,device)=>{const key=pick(device,['Categoria'],'Sin categoría');if(!map[key])map[key]=[];map[key].push(device);return map},{}),[devices]);
 async function action(type){if(type==='delete'&&!window.confirm('¿Eliminar este mantenimiento y todos sus dispositivos?'))return;if(type==='finalize'&&!window.confirm('¿Finalizar este mantenimiento? Después quedará en modo consulta.'))return;setWorking(type);setError('');try{if(type==='finalize')await requestAvailable(MODULE_ROUTES.maintenance.finalize,{maintenanceId},sessionToken);if(type==='reopen')await requestAvailable(MODULE_ROUTES.maintenance.reopen,{maintenanceId},sessionToken);if(type==='delete'){await requestAvailable(MODULE_ROUTES.maintenance.delete,{maintenanceId},sessionToken);navigate('/mantenimientos');return}if(type==='sheet'){const result=await requestAvailable(MODULE_ROUTES.maintenance.reportSpreadsheet,{maintenanceId},sessionToken);window.open(pick(result,['spreadsheetUrl','url']),'_blank','noopener,noreferrer')}if(type==='slides'){const result=await requestAvailable(MODULE_ROUTES.maintenance.reportSlides,{maintenanceId},sessionToken);window.open(pick(result,['slidesUrl','url']),'_blank','noopener,noreferrer')}await load()}catch(err){setError(err.message)}finally{setWorking('')}}
 if(loading)return <div className="page"><div className="state-card state-card--loading"><Icon name="progress_activity"/>Cargando mantenimiento...</div></div>;
 if(!data)return <div className="page"><div className="empty-state"><Icon name="error"/><h2>No se encontró el mantenimiento</h2><p>{error}</p></div></div>;
 return <div className="page maintenance-detail-page"><div className="page-header knowledge-detail-header"><button className="icon-button" type="button" onClick={()=>navigate('/mantenimientos')}><Icon name="arrow_back"/></button><div><span className="eyebrow">Mantenimiento técnico</span><h1>{pick(row,['TituloMantenimiento'],'Mantenimiento')}</h1></div>{status==='PENDIENTE'&&canEdit?<Link className="icon-button" to={`/mantenimientos/${encodeURIComponent(maintenanceId)}/editar`} aria-label="Editar"><Icon name="edit"/></Link>:<span/>}</div>{error&&<div className="alert alert--error"><Icon name="error"/><span>{error}</span></div>}<section className="maintenance-detail-summary"><div className="maintenance-detail-summary__hero"><span className="maintenance-card__icon"><Icon name="engineering"/></span><div><span className={`status-chip ${status==='FINALIZADO'?'status-chip--active':'status-chip--pending'}`}>{status}</span><h2>{pick(row,['Cliente','ClienteRef'],'Sin cliente')}</h2><p>{pick(row,['DescripcionGeneral'],'Sin descripción general')}</p></div></div><div className="maintenance-detail-summary__grid"><div><Icon name="calendar_month"/><span>Fecha</span><strong>{date(pick(row,['Fecha']))}</strong></div><div><Icon name="event_available"/><span>Finalización</span><strong>{date(pick(row,['FechaFinalizacion']))}</strong></div><div><Icon name="location_on"/><span>Ubicación</span><strong>{pick(row,['Ubicacion'],'Sin ubicación')}</strong></div><div><Icon name="groups"/><span>Responsables</span><strong>{pick(row,['Responsables','Responsable'],'Sin responsables')}</strong></div></div></section><section className="maintenance-report-actions"><button type="button" className="button button--secondary" onClick={()=>action('sheet')} disabled={Boolean(working)}><Icon name="table_view"/>{working==='sheet'?'Generando...':'Crear Excel'}</button><button type="button" className="button button--secondary" onClick={()=>action('slides')} disabled={Boolean(working)}><Icon name="slideshow"/>{working==='slides'?'Generando...':'Crear presentación'}</button>{pick(row,['SpreadsheetURL'])&&<a className="button button--ghost" href={pick(row,['SpreadsheetURL'])} target="_blank" rel="noreferrer"><Icon name="open_in_new"/>Excel creado</a>}{pick(row,['SlidesURL'])&&<a className="button button--ghost" href={pick(row,['SlidesURL'])} target="_blank" rel="noreferrer"><Icon name="open_in_new"/>Presentación creada</a>}</section><div className="maintenance-detail-groups">{Object.entries(grouped).map(([category,rows])=><section className="maintenance-category-section" key={category}><div className="maintenance-category-section__heading"><span><Icon name={getMaintenanceCategory(category).icon}/></span><div><span className="eyebrow">Categoría</span><h2>{category}</h2></div><strong>{rows.length}</strong></div><div className="maintenance-detail-device-grid">{rows.map(device=>{let answers={};try{answers=typeof device.RespuestasJSON==='string'?JSON.parse(device.RespuestasJSON||'{}'):device.RespuestasJSON||{}}catch{answers={}}const config=getMaintenanceCategory(category),images=device.Imagenes||[];return <article className="maintenance-detail-device" key={pick(device,['EvidenciaMantenimientoID','id'])}><header><div><span className="eyebrow">{pick(device,['Zona'],'Sin ubicación')}</span><h3>{pick(device,['NombreDispositivo'],'Dispositivo')}</h3><p>{[pick(device,['Modelo']),pick(device,['Serie'])].filter(Boolean).join(' · ')||'Sin modelo o serie'}</p></div><span className={`maintenance-device-state ${answerClass(pick(device,['Estado']))}`}>{pick(device,['Estado'],'Sin estado')}</span></header><div className="maintenance-answer-grid"><div className={answerClass(pick(device,['Funcionamiento']))}><span>Funcionamiento</span><strong>{pick(device,['Funcionamiento'],'Sin responder')}</strong></div><div className={answerClass(pick(device,['EnUso']))}><span>En uso</span><strong>{pick(device,['EnUso'],'Sin responder')}</strong></div>{config.questions.map(([key,label])=><div className={answerClass(answers[key])} key={key}><span>{label.replace(/^¿|\?$/g,'')}</span><strong>{answers[key]||'Sin responder'}</strong></div>)}</div>{pick(device,['Observacion'])&&<div className="maintenance-observation"><Icon name="notes"/><p>{pick(device,['Observacion'])}</p></div>}<div className="maintenance-detail-images">{images.map(image=><figure key={pick(image,['FotoDispositivoID','id'])}><img src={pick(image,['PreviewURL','DriveURL','url'])} alt={pick(image,['Nombre'],'Evidencia')}/><figcaption><strong>{pick(image,['Tipo'],'Evidencia')}</strong><span>{pick(image,['Nota'],'Sin nota')}</span></figcaption></figure>)}{!images.length&&<div className="muted-copy">Sin fotografías.</div>}</div></article>})}</div></section>)}</div><section className="maintenance-detail-footer-actions">{status==='PENDIENTE'&&canFinalize&&<button className="button button--primary" type="button" onClick={()=>action('finalize')} disabled={Boolean(working)}><Icon name="task_alt"/>Finalizar mantenimiento</button>}{status==='FINALIZADO'&&isAdmin&&<button className="button button--secondary" type="button" onClick={()=>action('reopen')} disabled={Boolean(working)}><Icon name="undo"/>Volver a pendiente</button>}{isAdmin&&<button className="button button--danger" type="button" onClick={()=>action('delete')} disabled={Boolean(working)}><Icon name="delete"/>Eliminar</button>}</section></div>
}
